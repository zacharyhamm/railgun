Create railway services pointing to docker containers, and spin/up down existing railway services.

From the root folder:

    npm i
    npm run build
    npm run start

railgun will be on http://localhost:3000
